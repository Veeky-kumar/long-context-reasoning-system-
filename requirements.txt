# Core dependencies
pandas>=2.0.0
requests>=2.28.0
tqdm>=4.65.0
numpy>=1.24.0

# Pathway (Track A requirement)
pathway>=0.7.0

# Semantic search (optional but recommended)
sentence-transformers>=2.5.0
scikit-learn>=1.3.0

# PyTorch - FIXED VERSION (compatible with sentence-transformers)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0